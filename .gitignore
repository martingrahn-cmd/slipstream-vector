# OS junk
.DS_Store
**/.DS_Store

# Editor / tooling
.claude/
.vscode/
.idea/

# Dependencies (this is a CDN import-map project with no build step, but
# future-proof in case a toolchain is added later)
node_modules/

# Martin's music is drop-in only — not committed.
# See assets/music/LÄGG-MUSIK-HÄR.txt for the slots; missing files = silence.
assets/music/*.mp3
assets/music/*.ogg
assets/music/*.wav
