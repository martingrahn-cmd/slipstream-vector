# OS junk
.DS_Store
**/.DS_Store

# Editor / tooling
.claude/
.vscode/
.idea/

# Dependencies (this is a CDN import-map project with no build step, but
# future-proof in case a toolchain is added later)
node_modules/

# (Music is committed — Martin's own compositions ship with the game.)
